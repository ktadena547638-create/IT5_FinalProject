# ==============================================================================
# Computer Inventory System (CIS) v1.0.1 - Dependencies
# ==============================================================================
#
# Course:       IT5L (2868)
# Professor:    Modesto C. Tarrazona
# Student:      Kenny Ray M. Tadena
# Date:         February 2026
#
# Installation: pip install -r requirements.txt
# ==============================================================================

# ===============================================================================
# Computer Inventory System (CIS) v1.0.1 - Locked/Recommended Dependencies
# ===============================================================================
# This file lists the packages required to run CIS. It contains two sections:
# - "Runtime" : packages needed for the application to run on another machine
# - "Dev/Test": optional developer tools used during development (not required
#               for an end-user install)
#
# Usage (create a fresh venv before installing):
#   python -m venv .venv
#   .venv\Scripts\pip install --upgrade pip
#   .venv\Scripts\pip install -r requirements.txt
#
# NOTE: tkinter and sqlite3 are part of the Python standard library and are
#       not listed here. On Windows, ensure a standard Python installation is
#       used (3.10+ recommended). For a fully reproducible environment use
#       the pinned versions below.
# ===============================================================================

# -----------------
# Runtime (pinned)
# -----------------
# These packages are required for the application to run.
altgraph==0.17.5
bcrypt==5.0.0
lxml==6.0.2
pillow==12.2.0
pywin32-ctypes==0.2.3
python-pptx==1.0.2
typing_extensions==4.15.0
xlsxwriter==3.2.9

# -----------------
# Development / Build / Test (optional)
# -----------------
# These packages are useful when developing, testing or building the EXE.
black==26.5.0
flake8==7.3.0
isort==8.0.1
mypy==2.1.0
pylint==4.0.5
pytest==9.0.3
pyinstaller==6.20.0
pyinstaller-hooks-contrib==2026.5

# -----------------
# Notes
# -----------------
# - If you plan to build the EXE on another machine, install the build
#   dependencies (pyinstaller and hooks). For a runtime-only install omit
#   the build/dev section.
# - If you need support for external DB backends (Postgres, MySQL), add the
#   relevant driver package (psycopg2-binary, mysql-connector-python).
# ===============================================================================
